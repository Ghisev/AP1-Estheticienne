{% extends 'base.html.twig' %}

{% block title %}Ajouter un contact{% endblock %}

{% block body %}
    <h1>Ajouter un contact</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Envoyer</button>
    {{ form_end(form) }}

    <div>
        <a href="{{ path('app_contact') }}">Retour</a>
    </div>
{% endblock %}
